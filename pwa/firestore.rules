rules_version = '2';

// Locked down by default. Phase 1 introduces per-event RBAC rules
// (roles granted per advance/event — see planning/ROADMAP.md § RBAC).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
